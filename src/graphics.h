/* This include renderer calls and all the things that needed to make a picture
 * 	in window.
 * IMPORTANT. This does not include anything relative to windows, it's only for
 * 	drawing purpose. Even swapping buffers isn't included, because it will
 * 	complicate tasks like drawing in separate windows or so.
 */

#ifndef graphics_h
#define graphics_h

#include "gltf.h"      // The renderer data structures

#include "opengl.h"             // TODO: Maybe change to more generic stuff

#endif /* graphics_h */
